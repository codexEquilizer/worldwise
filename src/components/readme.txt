This folder consists of only the navigation part of components for entire application.

Each pages are then handled in its own dedicated jsx file for each page.

App.jsx is where we have defined the path for each routing elements.
